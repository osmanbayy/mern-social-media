/** Sohbet görünümü panellerinde bölüm başlığı */
export default function SectionTitle({ children }) {
  return (
    <p className="text-[11px] font-bold uppercase tracking-wider text-base-content/40">{children}</p>
  );
}
